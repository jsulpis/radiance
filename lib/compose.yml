services:
  test:
    build:
      context: ..
      dockerfile: lib/Dockerfile
    ports:
      - "9323:9323"
    volumes:
      - ./test-results:/app/lib/test-results
      - ./tests/__screenshots__:/app/lib/tests/__screenshots__
